import { Row, Col } from "react-bootstrap";
import LoginRegisterTemplate from "../Template/LoginRegisterTemplate";

const Login = () => {
    return (
        <LoginRegisterTemplate>
            <Row>
                <Col>
                    <Row className="">Login Size Page</Row>
                </Col>
            </Row>
        </LoginRegisterTemplate>
    );
};

export default Login;
